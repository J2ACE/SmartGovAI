# Local development database only. Copy this file to deployment/.env before starting Docker.
POSTGRES_DB=smartgovai
POSTGRES_USER=smartgov
POSTGRES_PASSWORD=replace-with-a-local-password
POSTGRES_PORT=5432
